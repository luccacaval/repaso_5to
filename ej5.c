#include <stdio.h>

int main (void) {
	int km1 = 70;
	int km2 = 150;
	int velocidad;
	printf("Ingrese la velocidad de los vehiculos");
	scanf("%d", &velocidad);
	int tiempo = (150 - 75) / velocidad * 2;
	int distiancia = tiempo * veleciodad;
}
